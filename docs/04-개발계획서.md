# 04. 개발계획서 — UTub

- 작성일: 2026-08-17
- 상태: **사용자 승인 완료** (2026-08-17)
- 근거 문서: docs/01~03
- 작성 방식: Claude·agy 독립 분석 → 비교·보완(수정 5건) → 사용자 승인

---

## 1. 개발 환경

| 항목 | 내용 |
|---|---|
| 개발 PC | Windows 11 |
| IDE | Android Studio 최신 안정판 |
| JDK | 17 이상 (Android Studio 내장 JBR 사용) |
| 빌드 | Gradle(Kotlin DSL) + AGP 최신 안정판 |
| SDK | compileSdk 36 / targetSdk 35+ / **minSdk 29** (갤럭시 S20~최신) |
| 테스트 기기 | 갤럭시 실기기(USB 디버깅) + 에뮬레이터(API 29, 최신) |
| 버전 관리 | git (로컬 저장소) |

## 2. 기술 스택 및 선정 근거

| 스택 | 선정 | 근거 |
|---|---|---|
| 언어 | Kotlin (2.2+, 최신 안정판) | Android 표준, 코루틴/Flow |
| UI | Jetpack Compose + Material 3 | 선언형 UI, 플레이어 상태 전환(미니↔전체) 관리 용이 |
| 재생 | AndroidX Media3 (ExoPlayer + MediaSession + MediaSessionService) 최신판 | 알림·잠금화면·블루투스·오디오포커스 통합 표준 |
| 추출 | **NewPipeExtractor 최신판** (구현 시점에 GitHub 최신 릴리스 확인 필수) | 비로그인 스트림/검색/피드 파싱의 사실상 표준. 구버전은 재생 실패 위험 |
| DB | Room (KSP) | 로컬 개인화 데이터, Flow로 UI 자동 갱신 |
| 설정 | DataStore(Preferences) | 백그라운드 모드·자동재생 등 설정 저장 |
| DI | Hilt | Service·ViewModel 주입 표준 |
| 이미지 | Coil (Compose) | 썸네일 비동기 로딩·캐싱 |
| HTTP | OkHttp (NewPipeExtractor의 Downloader 구현에 사용) | Rate-Limit 인터셉터 구현. ※ Retrofit은 자체 서버 API가 없어 미사용 (agy 초안에서 제거) |
| 단위테스트 | JUnit5 + MockK + Turbine + Robolectric | 4단계 단위테스트 |

## 3. 아키텍처 / 프로젝트 구조

**단일 Gradle 모듈(:app) + 패키지 레이어링** (개인 프로젝트 빌드 단순화 — agy의 멀티모듈안 대신 채택. 교체 용이성은 인터페이스로 확보)

```
app/src/main/java/com/utub/
├─ extractor/                  # N-03 핵심: 추출 엔진 격리
│   ├─ StreamExtractor.kt      #   interface: resolveStream(videoId), search(q),
│   │                          #   suggest(q), trending(), channel(id), playlist(id)
│   ├─ newpipe/                #   NewPipeExtractor 구현체 + OkHttp Downloader
│   └─ RateLimiter.kt          #   요청 간 최소 간격 + 429 지수 백오프
├─ playback/                   # Media3 서비스 계층
│   ├─ PlaybackService.kt      #   MediaSessionService, FGS(mediaPlayback)
│   ├─ QueueManager.kt         #   대기열, 자동재생(연관 영상), 복원
│   ├─ NotificationProvider.kt #   MediaStyle + X(종료) 커맨드
│   └─ AudioModeController.kt  #   오디오/비디오 모드, 화면꺼짐 Surface detach
├─ data/
│   ├─ db/                     #   Room: Entity/DAO/Database
│   ├─ prefs/                  #   DataStore 설정
│   └─ repository/             #   Extractor+DB 조합 리포지토리
├─ ui/
│   ├─ home/ search/ player/ queue/ settings/ onboarding/
│   ├─ shared/                 #   VideoCard, MiniPlayer, 상태 컴포넌트
│   └─ theme/
└─ share/ShareReceiverActivity.kt  # ACTION_SEND + ACTION_VIEW(링크 탭) 수신
```

- UI(Compose) → ViewModel(StateFlow) → Repository → {StreamExtractor | Room | DataStore}
- PlaybackService는 Repository를 통해서만 추출 호출. UI와는 MediaController로 통신

## 4. 데이터 모델 (Room)

**1차 MVP 엔티티**

| Entity | 필드 | 비고 |
|---|---|---|
| QueueItem | id(PK), videoId, title, channel, thumbUrl, durationMs, orderIndex | **스트림 URL 저장 금지**(만료됨) — 재생 시점 재추출 (agy 초안 수정) |
| RecentPlay | videoId(PK), title, channel, thumbUrl, durationMs, lastPositionMs, playedAt, isCompleted | 간이 홈 "최근 재생" + 2차 시청기록/이어보기의 기반 |
| SearchHistory | query(PK), searchedAt | |

**2차 추가**: Subscription(channelId PK, title, avatarUrl, addedAt), FeedItem(캐시), Playlist / PlaylistItem(N:M), WatchLater, Liked, Download(파일 경로·품질)
**3차 추가**: Bookmark(videoId, positionMs, label), ChannelPref(채널별 배속)

**DataStore 설정 키**: backgroundMode(ALWAYS/HEADSET/OFF), autoplayRelated(bool), defaultAudioMode(bool), sleepTimer, clipboardDetect(bool), onboardingDone(bool)

## 5. 핵심 로직 설계

1. **스트림 추출 파이프라인**: videoId → 메모리 캐시(TTL 10분) 확인 → NewPipe 파싱(IO 디스패처) → 비디오+오디오/오디오전용 스트림 선별 → MediaItem 구성 → ExoPlayer. 실패 시 1회 재시도 후 ErrorState.
2. **서비스 수명주기**: MediaSessionService + FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK. onTaskRemoved: 재생 중이면 유지, 일시정지면 stopSelf. 알림 X 커맨드 → stop+release. Media3 setWakeMode(NETWORK)로 WakeLock 자동 관리.
3. **오디오/비디오 모드**: 화면 꺼짐(SCREEN_OFF) → Surface detach(오디오 지속), 켜짐 → reattach. 수동 오디오 모드 → 오디오 전용 트랙 선택(위치 동기 유지).
4. **백그라운드 재생 모드 설정 반영**: OFF면 SCREEN_OFF/onPause 시 pause. HEADSET이면 오디오 출력 장치 검사 후 결정. ALWAYS(기본)면 유지.
5. **오디오 포커스**: Media3 setAudioAttributes(handleAudioFocus=true) — 전화 시 일시정지·종료 후 재개, 덕킹 자동 처리. becomingNoisy(이어폰 분리) → pause.
6. **429/봇 확인 대응**: 전역 RateLimiter(요청 간 최소 250ms) + 429 시 지수 백오프(1s→2s→4s, 최대 3회) + 검색 디바운스 300ms.
7. **대기열 복원**: 종료 시 QueueItem + 현재 인덱스/위치 저장 → 재실행 시 목록 복원(스트림은 재생 직전 재추출).
8. **취침 타이머**: 만료 3초 전부터 볼륨 페이드아웃 → pause → 서비스 정리.

## 6. 1차 MVP 작업 분해 (WBS)

| Step | 작업 | 완료 기준 |
|---|---|---|
| 1 | 프로젝트 셋업: Gradle/Hilt/Room/DataStore/테마, R8 keep 규칙(NewPipe·Rhino) | 빌드·실행 성공 |
| 2 | extractor 패키지: StreamExtractor 인터페이스 + NewPipe 구현 + RateLimiter | 단위테스트: URL 파싱·검색·추출 (네트워크 목 포함) |
| 3 | playback 패키지: PlaybackService, 알림(SCR-800), 대기열, 오디오포커스, 모드 전환 | 실기기: 화면 꺼짐 재생 유지·잠금화면 제어·X 종료 |
| 4 | 온보딩(SCR-000)·공유/링크 수신(SCR-110)·클립보드 감지 | 유튜브 앱 공유 → 즉시 재생 |
| 5 | 검색(SCR-200)·간이 홈(SCR-010) | 검색→재생, 최근 재생 표시 |
| 6 | 플레이어 UI(SCR-300/310/320): 전체·미니·대기열, 제스처, 배속/자막/화질 | 미니↔전체 전환, 대기열 편집 |
| 7 | 설정 최소판(SCR-700)·통합 안정화: 1시간 연속 재생, One UI 딥슬립 검증 | MVP 완료 기준 충족 |

- 순서는 의존성 순. 각 Step 완료 시 단위테스트 통과 확인 후 다음 진행 (docs/05 테스트케이스 기준)
- MVP 완료 기준: "공유→광고 없는 재생→화면 꺼짐 1시간 연속 재생→잠금화면 제어·종료"가 실기기에서 안정 동작

## 7. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 유튜브 규격 변경으로 추출 실패 | StreamExtractor 격리 → NewPipeExtractor 버전 업데이트만으로 대응. UI에 안내 문구 |
| One UI 딥 슬립으로 서비스 킬 | FGS mediaPlayback + 배터리 최적화 예외 온보딩 + Step 7 실기기 장시간 검증 |
| R8 난독화로 NewPipe(Rhino JS) 런타임 오류 | keep 규칙 사전 반영, release 빌드 조기 검증 |
| 429/봇 확인 | RateLimiter + 백오프 + 디바운스 (5절 6항) |
| 스트림 URL 만료 | URL 비저장 원칙, 재생 직전 추출 (4절) |
| Windows 환경 빌드 이슈(경로 공백 "01. Devproject") | Gradle 경로 이슈 발생 시 프로젝트를 공백 없는 하위 폴더에 생성해 회피 |

## 8. 2·3차 단계 계획 (개요)

- 2차: 홈 피드·채널·구독 탭(FeedItem 캐시+주기 갱신), 보관함(기록/나중에/좋아요/재생목록), 다운로드(Media3 DownloadService), Takeout 임포트(CSV 파싱+배치)
- 3차: PIP, SponsorBlock(공개 API), 구간 북마크, 백업/복원(JSON), 채널별 배속
- 상세 WBS는 각 단계 착수 시 본 문서에 추가
